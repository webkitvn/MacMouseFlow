## 1. Native build and public seams

- [x] 1.1 Add the minimal macOS Swift build/test structure for App, Bridge, and Platform at the existing ownership boundaries; verify it targets macOS 14, imports CoreGraphics/AppKit, links the existing C ABI, and runs through `just test`.
- [x] 1.2 Verify deterministic public synthetic `CGEvent` coverage for scroll-only scope, LineBased/PixelBased classification, Axis1→vertical and Axis2→horizontal mapping, and signed preservation; verify Source Class `Unknown` through the established bridge ABI contract/code evidence without adding a test-only production seam. Evidence: `ScrollRuntimeTests.testLineBasedAxesReverseThroughABI` and `testPixelBasedAndUnexpectedEventsArePreserved` pass via `just test`; `PointerInputEngine.evaluate` constructs the exact-layout event with `source_class=Unknown` and zero reserved fields. Lifecycle notifications are owned by 3.2, not this deterministic adapter task.

## 2. Bounded adapter and ABI integration

- [x] 2.1 Implement LineBased extraction into exact-layout ABI values, bypass PixelBased and unexpected events unchanged, and keep phase/momentum opaque; verify the deterministic extraction tests pass. Evidence: `ScrollRuntimeTests.testLineBasedAxesReverseThroughABI`, `testPixelBasedAndUnexpectedEventsArePreserved`, and `testPhaseAndMomentumDoNotAffectLineEligibility` passed via `just test` on 2026-09-10; extraction maps Axis1/2 through the real ABI and these public synthetic CGEvent tests cover LineBased mutation, PixelBased/unexpected bypass, and phase/momentum-independence.
- [x] 2.2 Integrate the real C ABI/Rust engine with owner-retained lifetime and pre-mutation status/decision validation; verify system/default Preserve and reverse `Axis1 +3`/`Axis2 -2`→`-3`/`+2` through established seams. Evidence: Swift public synthetic tests cover system Preserve/reverse; Rust/C public ABI tests cover non-success/fail-open outputs including invalid layouts, null inputs, overflow, and panic containment; `PointerInputEngine.evaluate` validates status/version/size/reserved/decision before `ScrollAdapter` can reach a setter. The runtime retains the engine for callback lifetime; no production fault-control seam is introduced. This composite evidence supports the universal product fail-open requirement without impossible native fault injection.
- [x] 2.3 Apply Replace in place by writing only integer DeltaAxis1/2; verify deterministic public-seam tests cover the expected canonical deltas, complete PixelBased preservation, phase/momentum independence, no project assertions about derived FixedPt/PointDelta values, and no partial mutation path. Evidence: `ScrollRuntimeTests` passed via `just test` on 2026-09-10: reverse Axis1 +3/Axis2 -2 becomes -3/+2; PixelBased axes preserve; phase/momentum does not affect eligibility. `ScrollAdapter` has only the two integer setters after all fallible ABI validation; it contains no FixedPt/PointDelta writes or post-setter failure branch.

## 3. Event-tap lifecycle

- [x] 3.1 Implement permission preflight plus active session `CGEventTap` creation, enablement, dedicated non-UI run-loop ownership, disablement, removal, invalidation, and teardown for a normal `scrollWheel` mask; verify normal lifecycle/restoration through live smoke and review fail-open startup/teardown ordering for unobservable native allocation failures. Evidence: `ScrollRuntime` preflights accessibility, fails creation/source/enable paths open, owns the session tap on its dedicated thread, and disables/removes/invalidates before owner release; Issue smoke evidence records real create/enable/run/teardown/restoration PASS ([smoke evidence](https://github.com/webkitvn/MacMouseFlow/issues/42#issuecomment-5616121205), [operator evidence](https://github.com/webkitvn/MacMouseFlow/issues/42#issuecomment-5616146079)).
- [x] 3.2 Implement bounded `tapDisabledByTimeout` and `tapDisabledByUserInput` notification handling without engine translation, UI/MainActor work, I/O, synchronous logging, parsing, locks, or unbounded recovery; verify the production callback branch by deterministic/static evidence. Evidence: `ScrollRuntime.callback` branches notifications before `ScrollAdapter`, increments bounded timeout state, invokes one `CGEvent.tapEnable`, and otherwise returns the original event; the established callback-body benchmark seam exercises the same production callback entry/branching without a new test seam. Real forced notification/re-enable behavior and re-enable latency are explicitly **NOT_PROVEN** under Issue resolution `5617496027`: no supported observable trigger exists, and the accepted split evidence keeps live smoke responsible for normal lifecycle/zero-timeout evidence only.

## 4. Acceptance evidence

- [x] 4.1 Add and run the public-API LineBased behavior check using only integer DeltaAxis1/2 setters; verify reversed behavior through `NSEvent(cgEvent:)` on hosted macOS 14 without asserting exact FixedPt/PointDelta values, or STOP if integer setters are insufficient and a project-owned conversion would be required. Hosted macOS 14 evidence passed at a776dd2.
- [x] 4.2 Replace the Issue #42 `just smoke` and `just benchmark` placeholders with canonical runners, add the macOS 14 deterministic behavior CI gate, and verify `just check`, `just test`, `just ci`, and `openspec validate --all --no-interactive` pass locally. Evidence: `just check`, `just test`, `just ci`, and `openspec validate --all --no-interactive` passed on 2026-09-10 at `db66d866`; hosted macOS 14 coherence passed at `a776dd2`, and `benchmark-ci` supplies the separate hosted regression gate. Reference acceptance measures production callback-body latency directly; live smoke independently owns real-tap lifecycle evidence.
- [x] 4.3 Run recorded live smoke and `just benchmark` on the reference Mac across 100,000 production callback-body invocations; record LineBased replacement, PixelBased preservation, zero `tapDisabledByTimeout`, p99 ≤ 500 µs, p99.9 ≤ 1 ms, and max ≤ 2 ms, otherwise leave strict acceptance `NOT_PROVEN`. Evidence: 2026-09-10 reference Mac MacBookPro17,1 / Apple M1 / 16GB / macOS 26.6.2 (25G83), AC power, Low Power Mode off: ABI+Rust p99 42ns; callback-body p99 84ns, p99.9 84ns, max 9708ns. Live smoke: two timeout-zero runs; Logitech M590 LineBased PASS, M1 trackpad PixelBased PASS, restoration PASS ([smoke evidence](https://github.com/webkitvn/MacMouseFlow/issues/42#issuecomment-5616121205), [operator evidence](https://github.com/webkitvn/MacMouseFlow/issues/42#issuecomment-5616146079)).
- [ ] 4.4 Map every Issue #42 acceptance criterion to concrete evidence, post the verified handoff to the Issue, and archive only after OpenSpec validation, coherence evidence, deterministic tests, live smoke, and reference-Mac benchmark all pass.
