## 1. Candidate provenance

- [x] 1.1 Add the canonical `just local-build` path that runs `just ci`, retains Rust 1.98.1 and Xcode 26.6 pins, builds `io.github.webkitvn.macmouseflow`, ad-hoc signs it, and verifies its signature on the macOS 26.6.2 reference Mac; verify from a clean checkout.
- [x] 1.2 Prove the static-linkage candidate runs after relocation outside the checkout with no checkout or `target/` dependency; if and only if that fails without ABI/toolchain/architecture change, implement embedded-dylib bundle-relative loading and inside-out signing; verify the selected artifact is self-contained and its executable exits 0 within 5 seconds.

## 2. macOS 26 local lifecycle (reference Mac)

- [x] 2.1 Implement active and rollback handling at `~/Applications/MacMouseFlow.app` and `~/Library/Application Support/io.github.webkitvn.macmouseflow/LocalShip/rollback/MacMouseFlow.app`, retaining exactly one canonical rollback bundle without ever deleting the existing rollback bundle before its replacement has actually committed (stage-then-promote, never delete-then-hope); verify a successful update replaces the active bundle only after candidate launch proof. Fixed: `install()`'s retain-known-good step now copies (never moves) the real active bundle into a disposable staging copy, then promotes that copy into the rollback slot via a stage-then-swap helper that never deletes the existing rollback bundle before the replacement commits; the real active bundle is untouched throughout regardless of outcome.
- [x] 2.2 Validate active and rollback bundle identity, expected executable, ad-hoc signature, and launch probe before mutation in every lifecycle action that touches the active bundle — install, rollback, and uninstall alike, not install alone; verify foreign or malformed content aborts without changing active, rollback, or configuration state in all three. Fixed: `rollback()` and `uninstall()` now both call `validate_bundle_identity` on any pre-existing active bundle before mutating it, aborting without mutation on failure, exactly like `install()`.
- [x] 2.3 Implement failed-candidate restoration and configuration evidence using existence, byte length, and SHA-256, with an opaque sentinel outside `LocalShip/` when config is absent; verify failure restores the known-good bundle and preserves byte-identical evidence.
- [x] 2.4 Exercise actual clean install and launch on the macOS 26.6.2 reference Mac; verify the installed canonical executable exits 0 within 5 seconds and record signature/install/launch evidence.
- [x] 2.5 Exercise update and failed-update rollback on the macOS 26.6.2 reference Mac; verify one rollback bundle, abort-without-mutation failures, restored known-good launch, and unchanged configuration evidence. Re-verified after 2.1/2.2/2.7 landed: real-machine clean install, update, genuine controlled failed update (exit-9 candidate, canonical-path probe catches it, automatic restoration confirmed), explicit rollback (now with a post-restoration canonical-path launch probe confirmed via `verify-active`), and uninstall, all with unchanged config-sentinel evidence; machine left clean.
- [x] 2.6 Exercise documented uninstall on the macOS 26.6.2 reference Mac; verify the active artifact is removed and report the remaining documented local state without touching runtime configuration.
- [x] 2.7 Implement the bounded terminal recovery bundle exception (design.md: "Bounded terminal recovery bundle on double filesystem failure"; lead decision, Issue #41): on a true double filesystem failure during a directory swap in `install()`'s retain-known-good step or `rollback()`'s active-swap step, preserve the bundle that would otherwise be destroyed at one documented, named path (`terminal_recovery_path()`), report that path in the structured failure result (`LifecycleResult.preserved_recovery_path`) without claiming the canonical target was preserved, and remove it automatically on the next successful lifecycle action (`_clear_pipeline_temp_state()`, called at every success return in `install`/`rollback`/`uninstall`). Added an explicit post-restoration launch probe at the canonical active path in `rollback()`, matching `install()`'s canonical-path-probe design; a probe failure triggers automatic restoration from the pre-swap backup without ever moving/risking that backup directly. Added focused regression tests fault-injecting the double-failure branch for both call sites (`test_install_retain_step_double_failure_preserves_recovery_bundle`, `test_rollback_double_failure_preserves_backup_and_reports_it_honestly`) and the rollback post-restoration-probe path, including its own nested double failure (`test_rollback_post_restoration_probe_failure_triggers_automatic_restoration`, `test_rollback_post_restoration_probe_failure_and_restore_failure_preserves_backup`). Added CI `verify-active` step immediately after `rollback` in `macos14-lifecycle`. Reviewer round 4 fix: the nested double failure inside `rollback()`'s post-probe restoration attempt was relocating the wrong artifact into the recovery slot — the lower-value bundle that had just failed the post-rollback probe, not the true known-good bundle — while also leaving a second, undocumented copy at the ad hoc backup path. `_restore_active_from_backup_after_probe_failure` now discards that failing candidate and relocates the already-probe-validated known-good bundle there instead, leaving no ad hoc backup behind; `LifecycleResult.preserved_recovery_path` now always points to a verified-launchable terminal recovery bundle in this branch. `_clear_pipeline_temp_state` now verifies the terminal recovery bundle's removal and returns a structured `CleanupResult`; `install`/`rollback`/`uninstall` all now surface a structured failure (never an unqualified success) if that cleanup fails. New/updated regression tests: `test_rollback_post_restoration_probe_failure_and_restore_failure_preserves_backup` (rewritten to assert the recovery bundle holds the true known-good bundle, no ad hoc backup remains, and it self-cleans on the next success) and `test_install_reports_cleanup_failure_instead_of_unqualified_success`.

**Superseded historical context:** this earlier planning-only triple-failure note predates the authoritative atomic lifecycle-record amendment (Issue #41 resolution comment 5661599780). Current status: all tasks are complete; Actions run `34830438933` at `aa5df3b295212f71f5cdf131c3cc13b09e641490` passed hosted lifecycle proof.

## 3. macOS 14 same-artifact lifecycle (hosted compatibility)

- [x] 3.1 On the repository's existing GitHub-hosted `macos-26` runner, independently build a candidate via the identical canonical `just local-build` procedure used in section 1/2 — never the physical macOS 26.6.2 reference Mac's own build — then transport that hosted-built artifact unmodified to the repository's existing GitHub-hosted `macos-14` runner; verify its SHA-256 digest and ad-hoc signature match before transport and again after transport. Proven by Actions run `34830438933` at `aa5df3b295212f71f5cdf131c3cc13b09e641490`: `local-ship-candidate` and `macos14-lifecycle` both passed current atomic-record behavior.
- [x] 3.2 Exercise install, launch-probe, update, failure rollback, and uninstall lifecycle compatibility for that hosted-built artifact on the hosted `macos-14` runner, using the same mechanical criteria as the reference-Mac lifecycle, including an explicit-rollback step followed by a canonical-path `verify-active` check; explicitly exclude TCC/Accessibility permission grants, live `CGEventTap` input capture, and strict latency/benchmark claims from this row. Proven by Actions run `34830438933` at `aa5df3b295212f71f5cdf131c3cc13b09e641490`, including the expected failed-update `verify-active` and explicit test-harness lifecycle-record resolution.
- [x] 3.3 Gate: the macOS 14 lifecycle-compatibility row SHALL PASS before this change may archive or close; if the hosted runner or the transported artifact is unavailable or incompatible, mark this row `NOT PROVEN` and return to Issue #41 for a lead decision — never substitute macOS 26.6.2 reference-Mac evidence for it, never describe the transported artifact as sharing the physical reference Mac's build provenance, and never lower this acceptance criterion. Hosted lifecycle proof PASS: Actions run `34830438933` at `aa5df3b295212f71f5cdf131c3cc13b09e641490` passed `local-ship-candidate` and `macos14-lifecycle`, including the expected failed-update operator-resolution flow.

## 4. Evidence reconciliation / validation / archive handoff

- [x] 4.1 Run `just ci` from a clean checkout and record its result alongside both the macOS 26 reference-Mac lifecycle evidence and the macOS 14 hosted-compatibility evidence; verify no benchmark or smoke gate is added unless a runtime seam changed. Proven by Actions run `34830438933` at `aa5df3b295212f71f5cdf131c3cc13b09e641490`; `verify` and `ci-gate` passed alongside both hosted lifecycle jobs.
- [x] 4.2 Run `openspec validate issue-41-local-ship-pipeline --strict --no-interactive`; verify all proposal artifacts are coherent and no locked exclusion has been crossed.
- [x] 4.3 Map each acceptance requirement to its macOS 26.6.2 reference-Mac evidence and, separately, its hosted macOS 26-to-macOS 14 compatibility evidence; record explicitly that the two rows are independent and neither substitutes for nor shares the provenance of the other; record the bounded terminal recovery bundle's evidence once 2.7 lands; mark any unavailable mandatory evidence `NOT PROVEN`; verify readiness for the mandatory archive workflow without archiving in this task. `EVIDENCE.md` maps the independent reference-Mac and hosted rows; current hosted lifecycle evidence is Actions run `34830438933` at `aa5df3b295212f71f5cdf131c3cc13b09e641490`.

## 5. Apple Silicon (arm64)-only architecture assertion (ADR-0006; PR #90 review comment, Issue #41)

New tasks added by a planning-only update; none of the checkboxes above were touched. macOS 14+ on Apple Silicon (`arm64`) only is now the settled support boundary through v1; Intel `x86_64`, Rosetta 2, and Universal Binary work are explicitly out of scope, not deferred implementation detail.

- [x] 5.1 Change the Rust FFI release build invoked by `just local-build`/`build_candidate()` in `scripts/local_ship.py` to target `aarch64-apple-darwin` explicitly (`cargo build -p pointer-input-ffi --release --locked --target aarch64-apple-darwin`) instead of the host's default target triple, updating the built-executable and linker-flag paths (`macos/Package.swift`'s `MMF_FFI_LINKAGE=static` path) accordingly; verify the build still succeeds on the macOS 26.6.2 reference Mac. Done: `RUST_TARGET_TRIPLE = "aarch64-apple-darwin"` added; `build_candidate()` passes `--target aarch64-apple-darwin` explicitly; `Package.swift` reads a new `MMF_FFI_TARGET_TRIPLE` env var (set only by `build_candidate()`) to locate cargo's per-triple output directory (`target/<triple>/<profile>/`), defaulting to the prior unqualified `target/<profile>/` path for every other build (dev builds, smoke, benchmark, coherence-check, `swift test`) so no unrelated build path changed. Real `just local-build` re-run on the macOS 26.6.2 reference Mac confirmed the explicit `--target` invocation in the build log and a successful static-linkage link.
- [x] 5.2 Add an explicit architecture assertion to `build_candidate()`/`prove_self_contained()` in `scripts/local_ship.py` that inspects the built executable directly (e.g. `lipo -archs` or `file`) and fails the build (never signs or proceeds) if it reports anything other than a thin `arm64` Mach-O, including a Universal Binary containing an `x86_64` slice; add a focused unit test asserting a non-`arm64`/Universal executable is rejected before signing. Done: `assert_thin_arm64_executable()` added and called in `build_candidate()` immediately after the built executable is located, before it is copied into the candidate bundle or made eligible for signing. New `tests/test_local_ship.py::ArchitectureAssertionTests` (4 tests, established-seam style like `verify_ad_hoc_signature`): accepts a genuine thin `arm64` executable; rejects a thin `x86_64` executable; rejects a Universal Binary containing both `arm64` and `x86_64` slices (via `lipo -create`) even though it contains a valid `arm64` slice; rejects a missing executable path.
- [x] 5.3 Add the same direct binary-architecture assertion to the `local-ship-candidate` job (hosted `macos-26`, immediately after `just local-build`) and to the `macos14-lifecycle` job (hosted `macos-14`, immediately after `verify-transport`/before `install`) in `.github/workflows/ci.yml`, so both hosted rows verify `arm64` by inspecting the artifact itself rather than inferring it from the `macos-26`/`macos-14` runner label. Done: added an explicit `lipo -archs` step to `local-ship-candidate` right after `just local-build` (defense-in-depth alongside the check already embedded in `build_candidate()`, for CI-log visibility), and split the `macos14-lifecycle` job's combined "verify-transport, then install" step into three: `verify-transport`, a new explicit `lipo -archs` step on the received artifact, then `install` — matching "immediately after verify-transport/before install" precisely. `tests/test_repository_contract.py`'s existing assertions on job/string presence remain satisfied unchanged.
- [x] 5.4 **Superseded historical evidence/context:** the original local-only verification note predates current hosted proof. Current status: all tasks are complete; Actions run `34830438933` at `aa5df3b295212f71f5cdf131c3cc13b09e641490` passed both hosted arm64 inspection surfaces and lifecycle flow.

## 6. Triple filesystem failure recovery narrowing (Issue #41 resolution comment 5658378644)

New tasks added by this planning-only update; none of the checkboxes above were touched. Scope: only the case where relocation into `terminal_recovery_path()` itself also fails after a true double filesystem failure (commit move fails AND automatic restoration fails AND that recovery relocation also fails). Contract recorded in `design.md` ("Resolution for a true triple filesystem failure") and `spec.md` ("Preserve failure safety and pre-v1 boundaries", "Triple filesystem failure during a directory swap" scenario).

- [x] 6.1 Audit and, where needed, fix `_replace_directory_content()`'s and `_restore_active_from_backup_after_probe_failure()`'s failure-to-relocate-into-`terminal_recovery_path()` branches in `scripts/local_ship.py` so each stops attempting any further relocation on that third failure, and each consistently sets `LifecycleResult.preserved_recovery_path` (or an equivalently named field) to the exact existing pipeline-owned surviving path — the same slot the failed relocation read from, itself never moved or deleted by that attempt — rather than leaving it `None`/unset while only mentioning the path in the free-text reason string. Done: `_replace_directory_content()`'s existing triple-failure fallback already set this correctly; reworded its message to add the "no further relocation attempt will be made" language and confirm the surviving path must be removed-and-verified on the next success. `_restore_active_from_backup_after_probe_failure()`'s own relocation-into-recovery `except OSError` branch had the bug: it returned `ReplaceResult(False, reason)` without the third `preserved_recovery_path` argument, leaving it `None` even though the reason text named `backup` as the surviving path. Fixed to pass `str(backup)` explicitly.
- [x] 6.2 Verify the structured failure result in this branch never claims the canonical target or the canonical terminal recovery bundle was preserved, and that the surviving path is removed and its removal verified by `_clear_pipeline_temp_state()` (or an equivalent successor) on the next successful lifecycle action, exactly as already proven for the terminal recovery bundle in task 2.7. Done: reviewed both branches' reason text — neither claims the canonical target or canonical terminal recovery bundle was preserved; both name only the actual surviving ad hoc path. That surviving path (`old_rb_backup`/`backup`, both members of `_TRANSIENT_SLOT_SUFFIXES`) is already swept by `_clear_pipeline_temp_state()`'s existing transient-slot loop, and (per task 7.3 below) that removal is now verified, not just attempted.
- [x] 6.3 Add focused regression tests in `tests/test_local_ship.py` that fault-inject a true triple failure (commit move fails, automatic restoration fails, AND relocation into `terminal_recovery_path()` fails) for both `install()`'s retain-known-good step and `rollback()`'s active-swap/post-probe-restoration step, asserting: no further relocation attempt occurs, the reported surviving path matches the actual on-disk location of the last recoverable known-good content, the canonical target and canonical terminal recovery bundle are never described as preserved, and the surviving path is removed-and-verified-removed by the next successful lifecycle action. Done: `test_install_retain_step_triple_failure_leaves_content_at_its_surviving_path` and `test_rollback_triple_failure_leaves_content_at_its_surviving_path` added; both fault-inject all three moves failing, assert `preserved_recovery_path` equals the surviving ad hoc path (not the terminal recovery path, which is asserted absent), assert the surviving content's SHA-256 matches the true known-good bundle, and assert a follow-up successful lifecycle action removes it.
- [x] 6.4 Re-run `just check`/`just test`, the real-machine lifecycle exercise, and `openspec validate --strict` after 6.1–6.3 land; update `EVIDENCE.md`'s triple-failure row from `NOT YET IMPLEMENTED / NOT PROVEN` to the concrete evidence gathered, without altering the already-proven double-failure evidence above it. Done: `just check` and `just test` pass; full Python suite 52/82 pass (test_local_ship.py alone: 52); real-machine `just local-build` → install → rollback → verify-active → uninstall re-exercised successfully, machine left clean; `openspec validate --strict` passes. `EVIDENCE.md` updated (see below).

Superseded planning-only note (PR #90 review comment #5658378644, Issue #41 resolution; this checkbox intentionally left `[x]` per instruction — see section 8 below): 6.1–6.4's self-cleaning-on-next-success treatment of the triple-failure surviving path, proven above, has since been superseded by a lead decision to instead enter a terminal BLOCKED state on a true triple filesystem failure: the surviving path is never automatically relocated, reused, or cleaned up, and every subsequent automated lifecycle action must refuse to proceed until an operator resolves it manually. `design.md` ("Resolution for a true triple filesystem failure: terminal BLOCKED state") and `spec.md` (new BLOCKED-state paragraph and "Automated lifecycle action refuses while a terminal BLOCKED state is outstanding" scenario) now record this narrower contract. The code proven by 6.1–6.4 (self-cleaning) is not yet updated to match; that is new implementation work, tracked in section 8, not a revision of 6.1–6.4's own evidence.

## 7. Oracle re-review fixes: uninstall/rollback validation gap, architecture enforcement scope, cleanup verification scope (Issue #41)

- [x] 7.1 Oracle fix #1: `uninstall()` must validate any existing rollback bundle's content before deleting a valid active bundle, aborting without mutation if the rollback bundle is malformed — mirroring the rollback-validation-before-mutation `install()` and `rollback()` already perform. Done: added the same `validate_rollback_content(rb)` pre-check to `uninstall()`, before its existing active-bundle validation; a malformed rollback now aborts uninstall with zero mutation. New test `test_uninstall_aborts_without_mutation_when_rollback_is_malformed`; verified on the real machine (corrupted the real rollback bundle's executable, confirmed `uninstall` reports `ok: false` with `"aborted: existing rollback bundle is malformed"` and leaves the real active bundle byte-identical).
- [x] 7.2 Oracle fix #2: enforce the thin-`arm64` architecture assertion at every lifecycle eligibility boundary (static candidate, active, rollback, and transport validation), not only inside `build_candidate()`, and ensure it runs before any launch probe; also make the CI synthetic failed-update candidate's architecture explicit rather than host-default. Done: added non-raising `validate_thin_arm64_executable()` and wired it into `validate_static_identity()` (used by every eligibility boundary that funnels through it — `install()`'s candidate check, `validate_bundle_identity()` for pre-existing active/rollback content, and `verify_transport()` for a received bundle) immediately after confirming the executable exists and strictly before `verify_ad_hoc_signature`/the launch probe that `validate_bundle_identity` runs afterward; refactored `assert_thin_arm64_executable()` (used by `build_candidate()`) to reuse it. `.github/workflows/ci.yml`'s `macos14-lifecycle` synthetic failed-update candidate now compiles with an explicit `-arch arm64` and is independently `lipo -archs`-checked before `install` is attempted. New test `test_validate_static_identity_rejects_non_arm64_executable_before_any_probe` asserts rejection with `guarded_launch_probe` mocked and confirmed never called.
- [x] 7.3 Oracle fix #3: every pipeline-owned transient/terminal-recovery cleanup removal performed by `_clear_pipeline_temp_state()` must be verified, not just the terminal recovery bundle's; success must never leave an undocumented path behind. Done: the transient-slot sweep loop now checks each candidate still exists after its `rmtree` attempt and collects any leftover into the same structured `CleanupResult` failure path the terminal recovery bundle already used, joining all leftover paths into one reason string. New test `test_cleanup_reports_failure_when_a_transient_slot_is_not_actually_removed` simulates a stray `.rollback-staging` slot whose removal silently fails and confirms `uninstall()` reports structured failure naming that exact path rather than unqualified success.
- [x] 7.4 **Superseded historical evidence/context:** this pre-hosted-run note preserves chronology only. Current status: all tasks are complete; Actions run `34830438933` at `aa5df3b295212f71f5cdf131c3cc13b09e641490` passed the hosted candidate and macOS-14 lifecycle jobs.

## 8. Terminal BLOCKED state on a true triple filesystem failure, superseding self-cleaning (PR #90 review comment #5658378644, Issue #41 resolution)

Implementation and tests are complete. The terminal BLOCKED contract is recorded in `design.md` ("Resolution for a true triple filesystem failure: terminal BLOCKED state") and `spec.md` ("Preserve failure safety and pre-v1 boundaries" and its BLOCKED-state scenario). Scope remained narrow: only the true-triple-filesystem-failure survivor's disposition changed; the double-failure terminal recovery bundle remains self-cleaning.

- [x] 8.1–8.4 **Superseded historical evidence:** marker and survivor-slot path-presence-only implementations, tests, and reference-Mac exercise are retained as history only. Issue #41 resolution comment 5661599780 supersedes them: `LocalShip/lifecycle.json` is authoritative; retained or malformed record state blocks before mutation or cleanup, with no path-name inference. Hosted CI #59 predates the lifecycle-record amendment and is not current evidence.

### Hosted CI evidence

Historical CI #58/#59 prove earlier heads. Current hosted lifecycle proof is Actions run `34830438933` at `aa5df3b295212f71f5cdf131c3cc13b09e641490`: `verify`, `macos14-behavior`, `local-ship-candidate`, `macos14-lifecycle`, and `ci-gate` all passed, including expected failed-update `verify-active` followed by test-harness lifecycle-record removal.

## 9. Atomic lifecycle coordination and recovery record (Issue #41 resolution comment 5661599780)

- [x] 9.1 Replace survivor-slot path inference with one atomic `LocalShip/lifecycle.json` record containing operation and survivor identity, created before mutation, retained on failure, and removed only after fully successful completion. Valid operations are exactly `install`, `rollback`, and `uninstall`; a non-null survivor must be an existing bounded pipeline-owned terminal-survivor location. Malformed, stale, or out-of-scope records fail closed before mutation or cleanup without a survivor claim; no path-name inference remains.
- [x] 9.2 Add nonblocking native advisory locking for install, rollback, and uninstall; contention returns `lifecycle already running` without mutation. Catch active-bundle `rmtree` failure during uninstall, stop cleanup, verify configuration evidence, and report observed active/rollback/recovery state without preservation claim. If a triple-failure survivor exists but record update fails, the immediate failure reports its exact survivor while the retained pre-mutation record blocks later automation without one.
- [x] 9.3 Add focused tests for lock contention, malformed/interrupted/stale record, record creation/update failure, triple-survivor update failure, nested triple failure identity, lower-value cleanup failure, and structured partial-uninstall failure. `test_local_ship.py` passes 60 tests.
- [x] 9.4 Re-run local lifecycle and canonical verification. Reference Mac: `just local-build` → clean install → update → rollback → uninstall passed with lifecycle record removed after every success; machine left with no active bundle or lifecycle record. `just check` and `just test` (90 Python tests plus Rust/Swift suites) pass. Hosted CI proof is now recorded in Actions run `34830438933` at `aa5df3b295212f71f5cdf131c3cc13b09e641490`.
- [x] 9.5 Hosted expected failed-update resolution: after the expected failed-update command returns failure, CI runs `verify-active`; only then the CI test harness explicitly removes pipeline-owned `LocalShip/lifecycle.json` and continues update/rollback/uninstall evidence. This is external operator resolution, not a lifecycle command or survivor inference. Proven by Actions run `34830438933` at `aa5df3b295212f71f5cdf131c3cc13b09e641490`.
