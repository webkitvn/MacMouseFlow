# Evidence mapping — issue-41-local-ship-pipeline

Worker-owned evidence artifact for task 4.3. Maps every acceptance requirement in
`specs/local-macos-ship-pipeline/spec.md` to its macOS 26.6.2 reference-Mac evidence
and, separately, its hosted macOS 26-to-macOS 14 compatibility evidence. Per the lead
decision recorded on Issue #41, these two rows are independently gathered and neither
substitutes for nor shares the provenance of the other (design.md: "Two independent
evidence rows: macOS 26.6.2 reference Mac, and hosted macOS 26-to-macOS 14
compatibility").

Baseline commit: `cabf05d986bdf9fe95e8ae477495ccd2eea15046`. Reference Mac: macOS
26.6.2 (this host). Last refreshed after a reviewer round fixing the bounded
terminal recovery bundle's own correctness: the nested double filesystem failure
inside `rollback()`'s post-probe restoration attempt was relocating the wrong
artifact (the lower-value bundle that had just failed the post-rollback probe, not
the true known-good bundle) into the recovery slot, while leaving a second,
undocumented copy at an ad hoc backup path. `_restore_active_from_backup_after_probe_failure`
now relocates the already-probe-validated known-good bundle there instead and
discards the failing candidate, leaving no ad hoc backup behind. `_clear_pipeline_temp_state`
now verifies the terminal recovery bundle's removal and returns a structured
result; `install`/`rollback`/`uninstall` all now surface a structured failure
rather than an unqualified success if that cleanup itself fails.

## Requirement: Produce a verified local development artifact

| Scenario | macOS 26.6.2 reference-Mac evidence | Hosted macOS 26-to-macOS 14 compatibility evidence |
|---|---|---|
| Clean checkout produces an eligible artifact | `just local-build` run from an isolated clean-checkout copy (rsync excluding `.git`/`target`/`macos/.build`): `just ci` passed, static-linkage self-containment proven (`otool -L` on the built executable shows only system/Swift-runtime dylibs, zero references to the checkout or `target/`; relocated-to-`/tmp` copy exits 0), candidate ad-hoc signed and `codesign --verify --deep --strict` + `flags=0x2(adhoc)`/`Signature=adhoc` both confirmed. | **NOT PROVEN** — `local-ship-candidate` job (`.github/workflows/ci.yml`, hosted `macos-26` runner) has never executed; no push or `workflow_dispatch` occurred this session. |
| Verification or signing fails | `verify_ad_hoc_signature`/`validate_static_identity` unit-tested (`tests/test_local_ship.py::BundleValidationTests`) to reject non-ad-hoc signatures, missing/malformed `Info.plist`, wrong bundle ID/executable. | **NOT PROVEN** — same job never run. |

## Requirement: Exercise the local artifact lifecycle

| Scenario | macOS 26.6.2 reference-Mac evidence | Hosted macOS 26-to-macOS 14 compatibility evidence |
|---|---|---|
| Install and launch | Real install to `~/Applications/MacMouseFlow.app`: `codesign -dv --verify` → "valid on disk / satisfies its Designated Requirement"; timed launch probe exit 0 in ~7ms (< 5s). | **NOT PROVEN** — `macos14-lifecycle` job (hosted `macos-14` runner) never run. |
| Update and rollback | Real second install created exactly one valid, signed rollback bundle via the protected stage-then-promote retain step (`install()`; `tests/test_local_ship.py::test_install_retain_step_never_deletes_existing_rollback_before_replacement_ready`). Explicit `rollback` (stage-then-swap, never deletes active before a ready replacement) restored it, then launch-probed the restored bundle at the canonical active path (never a staging proxy) before reporting success (`tests/test_local_ship.py::test_rollback_restores_known_good_bundle`, `test_rollback_post_restoration_probe_failure_triggers_automatic_restoration`); verified with `verify-active`. | **NOT PROVEN**. CI's `macos14-lifecycle` job now includes a `verify-active` step immediately after `rollback` (mechanism only; not yet observed remotely). |
| Uninstall | Real `uninstall` validates the active bundle's identity/signature/launch probe before removing it (aborting without mutation if foreign or malformed, exactly like `install`), then removes it and reports remaining state `{"active_removed": true, "rollback_bundle_present": true, "terminal_recovery_bundle_present": false, "local_ship_dir_present": true, "support_dir_present": true}`; config sentinel byte-identical throughout. | **NOT PROVEN**. |

## Requirement: Prove same-artifact lifecycle compatibility on hosted macOS 14

| Scenario | macOS 26.6.2 reference-Mac evidence | Hosted macOS 26-to-macOS 14 compatibility evidence |
|---|---|---|
| Same artifact transported and verified | N/A — this row is specifically about the hosted-to-hosted chain, not the reference Mac. `package_for_transport`/`verify_transport`'s SHA-256 + ad-hoc-signature round-trip mechanism is unit-tested locally (`tests/test_local_ship.py::TransportVerificationTests`), proving the mechanism is correct. | **NOT PROVEN** — this is the acceptance row itself: it requires an observed run of `local-ship-candidate` (hosted `macos-26`, independent build) → `macos14-lifecycle` (hosted `macos-14`) on GitHub's real infrastructure, which has not occurred. Per the lead decision, this row never claims the physical reference Mac's build provenance; it proves the canonical procedure's hosted-built artifact is macOS-14-compatible. |
| Hosted macOS 14 lifecycle compatibility passes | N/A. | **NOT PROVEN**. |
| Hosted macOS 14 unavailable or incompatible | N/A. | This row's actual status **for this change**: NOT PROVEN because no remote run was authorized/attempted, not because the runner was observed unavailable. Per design.md/tasks.md 3.3, this blocks archive and returns to Issue #41 for a lead decision. |

## Requirement: Preserve failure safety and pre-v1 boundaries

| Scenario | macOS 26.6.2 reference-Mac evidence | Hosted macOS 26-to-macOS 14 compatibility evidence |
|---|---|---|
| Failed replacement preserves state | Real genuine controlled failed update: an ad-hoc-signed, identity-valid candidate with `exit code 9` was committed to the canonical active path, then failed its canonical-path probe → `"canonical-path launch probe failed: nonzero exit status 9; automatically restored the known-good bundle to the canonical active path"`; active SHA-256 confirmed restored; config sentinel unchanged. Plus unit tests for a config-integrity-violation `SystemExit` surfacing *after* canonical commit, filesystem errors during install/rollback, and the rollback post-restoration probe failing (`test_config_mutation_after_canonical_replacement_restores_known_good`, `test_config_mutation_after_first_install_replacement_leaves_active_empty`, `test_filesystem_error_installing_candidate_is_reported_and_restorable`, `test_rollback_filesystem_error_while_{staging,replacing}_preserves_active`, `test_rollback_post_restoration_probe_failure_triggers_automatic_restoration`). | The workflow's dedicated "genuine controlled failed-update rollback" step exists in `.github/workflows/ci.yml` (hosted `macos-14`) but **NOT PROVEN** — never executed remotely. |
| Active/rollback/uninstall mutation is gated on validation, not install alone | Real test + unit tests: `rollback()` and `uninstall()` both now call `validate_bundle_identity` on any pre-existing active bundle before mutating it, aborting without mutation on failure — `tests/test_local_ship.py::test_rollback_aborts_without_mutation_when_active_is_foreign`, `test_uninstall_aborts_without_mutation_when_active_is_foreign` — closing the gap where only `install()` validated. | N/A — proven by the local unit tests regardless of host; not part of the same-artifact lifecycle-compatibility criterion. |
| Rollback slot promotion never deletes the existing rollback bundle before its replacement is ready | Fixed and evidenced: `install()`'s retain-known-good step copies (never moves) the real active bundle into a disposable staging copy, then promotes that copy into the rollback slot via the shared stage-then-swap helper (`_replace_directory_content`), which never deletes the existing rollback bundle before the replacement commits. Regression test `test_install_retain_step_never_deletes_existing_rollback_before_replacement_ready` fault-injects the promotion move failing and confirms the existing rollback bundle's SHA-256 is unchanged and the real active bundle (never moved during this step) is untouched. | N/A — proven by the local unit test regardless of host. |
| Rollback double failure (commit move AND automatic restoration both fail) never destroys the last recoverable copy, and is never a second persistent slot | Fixed and evidenced under the bounded terminal recovery bundle exception (design.md; lead decision, Issue #41): when both the staged→active commit move and the backup→active restoration move fail, the content that would otherwise be destroyed is relocated to the single, documented `terminal_recovery_path()` (`~/Library/Application Support/io.github.webkitvn.macmouseflow/LocalShip/terminal-recovery/MacMouseFlow.app`) — never left at an ad hoc path and never silently deleted. `LifecycleResult.preserved_recovery_path` reports that exact path; the reason states `"is MISSING, not preserved"` and never claims restoration in this branch. The same protection applies to `install()`'s retain-known-good promotion step. The recovery bundle is not version history: `_clear_pipeline_temp_state()` removes it automatically on the very next successful lifecycle action (any of install/rollback/uninstall), and now verifies that removal actually took effect rather than assuming it did (see the cleanup-verification row below). Regression tests: `test_rollback_double_failure_preserves_backup_and_reports_it_honestly` (also confirms the next successful `install()` clears the recovery bundle), `test_install_retain_step_double_failure_preserves_recovery_bundle`. | N/A — filesystem fault injection of this kind is not meaningfully distinct on hosted macOS 14 from macOS 26.6.2; proven by the local unit tests regardless of host, and not part of the same-artifact lifecycle-compatibility criterion. |
| Explicit rollback proves the restored bundle launches at the canonical active path, not just the rollback source | Fixed and evidenced: `rollback()` now runs `guarded_launch_probe` at the canonical active path (never a staging proxy) immediately after the swap commits, before reporting success — mirroring `install()`'s canonical-path-probe design. A probe failure triggers automatic restoration from the pre-swap backup via a disposable copy, so the backup itself is never put at risk during that restoration attempt. Regression tests: `test_rollback_post_restoration_probe_failure_triggers_automatic_restoration`. Real-machine exercise confirmed the happy path (`verify-active` passes immediately after `rollback`). | CI's `macos14-lifecycle` job now runs `verify-active` immediately after `rollback` (mechanism only; **NOT PROVEN** remotely). |
| A nested double failure inside rollback's post-probe restoration attempt relocates the true known-good bundle, not the failing candidate, to the one terminal recovery slot | Fixed and evidenced this round (reviewer finding): the first implementation of this nested case incorrectly relocated whichever bundle the generic swap helper had displaced from `active` — the lower-value bundle that had just failed the post-rollback probe — into `terminal_recovery_path()`, while also leaving a second, undocumented copy of the true known-good bundle at the ad hoc `.rollback-backup` path. `_restore_active_from_backup_after_probe_failure` now discards that failing candidate outright and moves the already-probe-validated known-good bundle (proven launchable by `validate_bundle_identity(active)` at the very top of `rollback()`, before it was ever copied aside) into the recovery slot instead, and no ad hoc backup remains afterward. `LifecycleResult.preserved_recovery_path` in this branch now always points to a verified-launchable bundle at `terminal_recovery_path()`; exactly one recovery-related artifact exists. Regression test `test_rollback_post_restoration_probe_failure_and_restore_failure_preserves_backup` (rewritten) asserts: the reason references the recovery path and not the old ad hoc backup path, no ad hoc backup exists, the recovery bundle exists, passes `validate_bundle_identity`, and its executable's SHA-256 matches the true known-good bundle's (not the failing candidate's); none of the transient staging/backup slots this attempt used survive; and the recovery bundle self-cleans on the next successful lifecycle action (`uninstall`, since `active` is missing and the deliberately-corrupted rollback bundle from this test's setup would otherwise abort a follow-up `install`). | N/A — proven by the local unit test regardless of host. |
| Pipeline cleanup after a successful lifecycle action cannot silently leave the terminal recovery bundle behind | Fixed and evidenced this round (reviewer finding): `_clear_pipeline_temp_state()` previously called `shutil.rmtree(..., ignore_errors=True)` on the recovery directory and returned nothing, so a real removal failure would have been silently swallowed while the caller still reported unqualified success. It now verifies the recovery bundle is actually gone and returns a structured `CleanupResult`; `install()`, `rollback()`'s happy path, and `uninstall()` all now check that result and report a structured failure (with `preserved_recovery_path` pointing at the still-present bundle) instead of `ok: true` when cleanup fails, even though the lifecycle mutation itself fully committed. Regression test `test_install_reports_cleanup_failure_instead_of_unqualified_success` fault-injects a real double failure to populate the recovery bundle, then simulates its removal silently not taking effect during a subsequent otherwise-clean install, and confirms the install reports `ok: false` with the recovery path, then confirms a further install with real cleanup restored actually clears it. | N/A — proven by the local unit test regardless of host. |
| Foreign or malformed copy aborts (install, rollback, and uninstall alike) | Real/unit tests: a foreign-bundle-ID active copy causes `install`, `rollback`, and `uninstall` to each abort with zero mutation (`test_install_aborts_without_mutation_when_active_is_foreign`, `test_rollback_aborts_without_mutation_when_active_is_foreign`, `test_uninstall_aborts_without_mutation_when_active_is_foreign`); malformed rollback content rejected without mutation. | **NOT PROVEN**. |
| Unsupported distribution request | No Developer ID, notarization, public distribution, quarantine removal, or new helper/daemon/process was added anywhere in this diff (verified by review of `scripts/local_ship.py`, `.github/workflows/ci.yml`, `Justfile`, `Package.swift`, `Cargo.toml`). | N/A — this is a design-time exclusion, not a runtime scenario requiring hosted evidence. |

## Summary

- **macOS 26.6.2 reference-Mac row: fully evidenced** for every requirement above, using genuine real-machine installs/updates/genuine-controlled-failed-updates/rollbacks/uninstalls plus targeted unit tests for fault-injection scenarios (including nested double failures and pipeline-cleanup failures) that are impractical to trigger organically on real hardware every run. All 43 tests in `tests/test_local_ship.py` pass, plus 73/73 across the full Python suite; `just check` and `just test` pass; `openspec validate --strict` reports the change valid.
- **Hosted macOS 26-to-macOS 14 compatibility row: NOT PROVEN for every requirement.** No commit was pushed and no `workflow_dispatch` was triggered this session (worker scope prohibits committing/pushing). The CI wiring (`local-ship-candidate` on hosted `macos-26`, `macos14-lifecycle` on hosted `macos-14`, including the `verify-active` step after `rollback`) is implemented and unit-tested at the mechanism level but has no observed remote execution. Per the lead decision, this row never claims the physical reference Mac's build provenance.
- Per design.md/tasks.md (task 3.3), this change **must not archive or close** until the hosted macOS 14 row is observed to PASS on a real GitHub Actions run, or a lead decision explicitly accepts/updates this acceptance criterion. This file does not lower that bar; it only makes the current evidence state explicit and auditable.
